# Enterprise Business Graph (EBG) v1.2 — Progress File
# Last updated: 2026-03-24
# Current phase: Phase 0 — Foundation (Sprint 1–3)

## What Has Been Done

### Phase 0 — Foundation (IN PROGRESS)
- [x] Repository setup and Git initialization
- [x] Docker Compose infrastructure (Neo4j 5 Enterprise, TimescaleDB/PG16, Kafka KRaft)
- [x] All Cypher DDL files written (01 through 08): constraints, indexes, node templates, edges, traversal queries, v1.2 seeds
- [x] SQL migrations written: configuration_settings (bi-temporal), TimescaleDB projections (6 tables)
- [x] TypeScript type system: all enums and interfaces in src/schema/neo4j/types.ts
- [x] Library layer: Neo4j driver, PG pool, Kafka producer/consumer, Pino logger, typed errors
- [x] Saga coordinator: executeSaga() with ordered steps + compensating rollback
- [x] GL projector: Kafka consumer → TimescaleDB gl_period_balances upsert
- [x] ConfigurationService: resolveConfig() with 4-level cascade, setConfig(), getConfigValue<T>()
- [x] Journal posting service: double-entry validation, period check, fund_id NFP check, 4-step saga
- [x] Graph CRUD service: Entity, Outcome, Activity, AccountingPeriod, Fund, CONTRIBUTES_TO edge
- [x] REST API: graph-routes, gl-routes, config-routes on Express :4000
- [x] All 11 invariant test files written (GL 1-5, Graph 1-2, Ontology 1-3, Equity 1)
- [x] Init scripts: neo4j-init.ts, pg-migrate.ts, seed.ts
- [x] TypeScript compiles cleanly (tsc)
- [x] CLAUDE.md with full documentation map and build instructions
- [x] All spec documents (7 docx files) and 9 markdown docs in place

### Not Yet Done (Phase 0 remaining)
- [ ] Run Docker services and verify connectivity
- [ ] Execute neo4j-init.ts against live Neo4j
- [ ] Execute pg-migrate.ts against live PostgreSQL/TimescaleDB
- [ ] Execute seed.ts to populate default config
- [ ] Run invariant tests against live databases (currently tests need running infra)
- [ ] Verify Kafka consumer projects a test LedgerLine to TimescaleDB
- [ ] Verify saga coordinator rollback on simulated failure
- [ ] Configure Neo4j deny UPDATE/DELETE on JournalEntry/LedgerLine labels
- [ ] Verify ConfigurationService cascade resolution end-to-end
- [ ] Confirm four Entity nodes exist with all v1.2 properties

## Architecture Decisions
- CQRS: Neo4j (write) + TimescaleDB (read) — enforced, never mix
- Append-only ledger: JournalEntry/LedgerLine never updated/deleted
- Bi-temporal on every JournalEntry: valid_time + transaction_time
- Configuration over code: all policy via ConfigurationService
- Ontology boundary: hard learning wall between FINANCIAL and MISSION

## What Comes Next
1. Finish Phase 0 "definition of done" items (live database verification)
2. Phase 1: Core Graph + Basic GL + Fund Accounting + Admin UI (Sprints 4-8)
3. Phase 2: Accruals + Fixed Assets + Depreciation Engine (Sprints 9-15)

## Known Issues
- Vitest requires Node 20+ (current env has 18.19.1); using tsc build as regression gate
- Docker services not yet started/verified
